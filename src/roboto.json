fetch('/roboto.json')
  .then(res => res.json())
  .then(data => {
    doc.addFileToVFS('Roboto-Regular.ttf', data.font);
    doc.addFont('Roboto-Regular.ttf', 'Roboto', 'normal');
    doc.setFont('Roboto');
    // PDF içeriği buraya
  });
